# היסטוריית שינויים (Changelog) - מעבדת חשמל 1

## תאריך: 30 ביוני 2026 (סשן נוכחי)

### 1. שדרוג תשתיות ושאלות H5P
* הוספה של 7 שאלות בנושא בטיחות לניסוי מספר 1.
* שילוב הספרייה `H5P.MathDisplay` בתוך קבצי התבנית של H5P.
* עדכון של כל סקריפטי יצירת ה-H5P (`generate_exp01_prelab_h5p.js`, `generate_exp02_prelab_h5p.js` ועוד) כך שיתמכו בתצוגת נוסחאות מתמטיות (LaTeX) תקינה ומקצועית במודל.

### 2. אריזה ובנייה (Build & Packaging)
* שינוי מנגנון האריזה האוטומטי (בסקריפט `package_moodle_experiments.py`) כך שקובץ ה-ZIP של כל ניסוי יכלול תיקייה נפרדת ונקייה של ה-H5P, המותאמת להעלאה למודל בפורמט ש-Moodle תומך בו.
* פקודת בנייה אחת מרוכזת: הסברנו כיצד הרצת `npm run release` מפעילה את `rebuild_and_clean_dist.py` שאורז את כל הפרויקט לספריית `dist/release`.

### 3. שדרוג טבלאות הניסויים ועיבוד נתונים ל-MATLAB
* התקבלה החלטה מקצועית כיצד לנהל איסוף נתונים: במקום טבלאות אינטראקטיביות מורכבות במודל (שנחסמות לעיתים קרובות), יעשה שימוש בקובץ תבנית של אקסל (.xlsx) שהסטודנטים יורידו ויטענו ל-MATLAB.
* נכתב והורץ סקריפט אוטומטי (`scripts/upgrade_tables.py`) שסרק את כל קבצי ה-HTML של הניסויים:
  * כל הטבלאות הומרו למחלקות Bootstrap מתקדמות (`table-bordered`, `table-striped`, `table-primary`).
  * נוספה תצוגה רספונסיבית לניידים.
  * בכל ניסוי הושתלה "תיבת הורדה" מעוצבת הקוראת לסטודנטים להוריד את קובץ האקסל עבור עיבוד ב-MATLAB.

### 4. תיקוני עיצוב, טקסט ורזולוציות (RTL / LTR)
* תוקן באג תצוגה מהותי בניסוי 1 (חלק A - זיהוי ומדידת קבלים) שבו המעבר בין עברית ל-LaTeX גרם למילים להתהפך (כמו "פירושו" שהפך ל-"ושוריפ"). 
* הטקסט שוכתב והוצמד בצורה נכונה עם תגיות `dir="ltr"` במקומות הנדרשים כדי לשמור על קריאות מושלמת.

---
**לסיכום:** המערכת כעת יציבה, יפה יותר, תומכת באופן מלא בנוסחאות מתמטיות ב-H5P, ומכינה את הקרקע לשילוב מושלם של עיבוד נתונים מקצועי ב-MATLAB על ידי הסטודנטים. הקבצים המוכנים למודל מחכים כולם בתיקיית `dist/release`.
