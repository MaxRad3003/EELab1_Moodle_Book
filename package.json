{
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "node -e \"require('fs').rmSync('dist', {recursive: true, force: true})\"",
    "release": "python scripts/rebuild_and_clean_dist.py"
  },
  "dependencies": {
    "i": "^0.3.7"
  },
  "devDependencies": {
    "juice": "^12.1.0"
  }
}
