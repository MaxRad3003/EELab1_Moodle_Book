{
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "node -e \"require('fs').rmSync('dist', {recursive: true, force: true})\""
  },
  "dependencies": {
    "i": "^0.3.7"
  },
  "devDependencies": {
    "juice": "^12.1.0"
  }
}
