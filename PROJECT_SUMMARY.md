<div dir="rtl">

# 📊 דוח התקדמות וסיכום פרויקט - מעבדה לחשמל 1

**תאריך עדכון:** 30 ביוני 2026

מסמך זה מרכז את כלל הפיתוחים, השדרוגים והתיקונים שבוצעו במערך האוטומציה ובהכנת תכני מעבדה לחשמל 1 עבור מערכת ה-Moodle וה-MATLAB Grader.

---

## 🎯 1. אינטגרציה עם MATLAB Grader (ניסויים 2-5)
בנינו תשתית מלאה ואוטומטית ליצירת משימות הכנה ופיתרון בניסויים מבוססי MATLAB. 
* **אוטומציה מלאה**: נוצר הסקריפט `generate_all_grader_tasks.js` שפורס אוטומטית את כל התבניות (קוד בסיס, פתרון רפרנס, מבחני בדיקה ותיאור משימה) עבור ניסויים 2 עד 5.
* **הפרדת שפות**: התבניות (קוד, הערות, מבחנים) נכתבו כולן באנגלית תקנית כפי שנדרש למערכת MATLAB Grader, בעוד שהנחיות המודל וה-Moodle Book נשארו בעברית מלאה.
* **Moodle Integration**: לכל משימה נוצר קוד HTML מוכן להעתקה לתוך המודל, אשר מסביר לסטודנט בדיוק כיצד לגשת למשימה ומפנה אותו לקישור ה-LTI המתאים.

## 📁 2. ארכיטקטורה וארגון מחדש של תיקיית הסקריפטים
תיקיית ה-`scripts` גדלה משמעותית ולכן בוצע ארגון מחדש (Refactoring) לסביבת עבודה מקצועית ונקייה:
* חילקנו את כל הסקריפטים ל-5 תת-תיקיות לוגיות:
  1. `build_and_package/` - סקריפטים שאורזים את ה-ZIP ובונים את הפרויקט.
  2. `html_and_links/` - סקריפטים שעושים ולידציה לקישורים, מתקנים טבלאות וכותרות.
  3. `assessments_and_h5p/` - אוטומציות ליצירת בחני H5P לפרקי ההכנה.
  4. `matlab_grader/` - מחולל התבניות של MATLAB.
  5. `utils/` - כלי עזר שונים (אופטימיזציית תמונות, סורקי ZIP).
* תיקנו את קובץ ה-`package.json` והוספנו **פקודות פרטניות** המאפשרות לבנות או לארוז ניסוי ספציפי בלבד (למשל `npm run build:exp04`), מבלי להמתין לבניית כל הפרויקט.
* מסמך התיעוד (`Scripts_Documentation.html`) עודכן לחלוטין ומשקף את המבנה החדש, כולל פירוט פקודות ההרצה.

## ⚡ 3. שדרוגי ביצועים (Performance Optimization)
ביצענו אופטימיזציה משמעותית לסקריפטים, שתורגמה ישירות לביצועים טובים יותר לסטודנטים במודל:
* **עיבוד מקבילי (Multi-threading)**: סקריפט התמונות (`optimize_images.py`) שוכתב כך שיעבוד בצורה מקבילית. מהירות ההמרה הואצה פי כמה מונים.
* **מעבר לפורמט WebP**: כל התמונות (34 במספר) הומרו אוטומטית לפורמט WebP החדשני. התוצאה: ירידה חדה במשקל הקבצים (עד פי 5 פחות מקום, למשל מ-220KB ל-45KB לתמונה). משקל ה-ZIP קטן משמעותית ומהירות טעינת הדפים במודל השתפרה דרמטית.
* כל קישורי ה-HTML תוקנו אוטומטית כך שיצביעו לתמונות ה-WebP החדשות.

## 🐛 4. תיקוני באגים ויציבות המערכת
* **תמיכה בקידוד עברית (UTF-8)**: הזרקנו קוד מערכתי לכל סקריפטי ה-Python שמוודא תמיכה מלאה בהדפסות בעברית למסוף (Terminal). זה פתר קריסות של סורק הקישורים שקרו כאשר נתקל בנתיב עם עברית (כגון "מעבדה חשמל 1").
* **תיקון נתיבי שורש (`__file__`)**: בעקבות חלוקת הסקריפטים לתיקיות, תיקנו את אלגוריתם חיפוש התיקייה הראשית (Root Path) ב-Python כך שיזהה נכון את תיקיית הפרויקט מכל תת-תיקייה שבה הוא נמצא.
* **ולידציה עוברת חלק**: תהליך ה-`npm run release` רץ כעת מאפס ועד 100 ללא אף שגיאת קישור או בנייה.

---

### המלצות להמשך עבודה
1. בחינה של תבניות ה-MATLAB Grader והזרנתן למערכת המכללה דרך רכיב ה-LTI במודל.
2. ניתן להשתמש בפקודות הפרטניות (`npm run package:exp04`) בכל פעם שאתה מעדכן טקסט קטן בניסוי מסוים, כדי לחסוך זמן בנייה.

</div>
