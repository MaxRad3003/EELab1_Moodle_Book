% 1. Measured Data
f_meas = [ ];
V_meas = [ ];

% 2. Find maximums
[V_max, idx] = max(V_meas);
f0_empirical = 

% 3. Half-power voltage
V_half_power = 

% 4. Plot (Optional but recommended)
semilogx(f_meas, V_meas, '-o');
grid on;
