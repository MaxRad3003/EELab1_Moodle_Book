# דוח סיכום שדרוג ואינטגרציית מעבדה לחשמל 1 ב-Moodle

דוח זה מסכם את כל עבודות הפיתוח, השדרוג והאינטגרציה שבוצעו בפרויקט עבור ארבעת הניסויים הראשונים (`Exp01` עד `Exp04`) וספר ציוד המדידה (`Measuring equipment`), במטרה להפוך את חומרי הלימוד לחוויית למידה דינמית, מעוצבת ונוחה לסטודנטים ולסגל ההוראה.

---

## 1. חידושים טכנולוגיים ואינטראקטיביים

### א. טבלאות מדידה דינמיות ושמירה מקומית (localStorage)
* **מילוי נתונים מקוון:** כל טבלאות מהלך הניסוי בניסוי 4 הוחלפו בתיבות קלט אינטראקטיביות בהן הסטודנט מזין את נתוני המדידה ישירות במודל.
* **מנגנון חסינות ואבטחה ברשת (`localStorage`):** הנתונים שהסטודנט מקליד נשמרים אוטומטית ברמת הדפדפן בזמן אמת. במקרה של רענון דף, קריסת אינטרנט או סגירת הדפדפן, המידע אינו הולך לאיבוד ומשוחזר אוטומטית בעת כניסה מחדש לדף.
* **שורת דוגמה מנחה:** בהתאם לעיקרון הפדגוגי, הוספנו שורת דוגמה פתורה המציגה לסטודנט כיצד לחשב ידנית את הערכים המבוקשים, בזמן שיתר השורות נשארות ריקות למילוי עצמי של הסטודנט.
* **ייצוא ל-Excel (CSV):** הסטודנטים יכולים להוריד את המדידות הגולמיות שלהם כקובץ CSV בלחיצת כפתור פשוטה.

### ב. עוזר סקריפטים ואינטגרציית MATLAB
* מתחת לכל טבלה מוטמע כפתור **"העתק קוד MATLAB ללוח"**.
* בלחיצה עליו מועתקים הנתונים שהוקלדו בטבלה כוקטורים של MATLAB, יחד עם **נוסחאות והנחיות חישוב כמשפטי הערה (Commented Out)**. הסטודנט לוקח את הקוד הביתה, משלים את הנוסחאות ומקבל שרטוט גרפים וחישובים מדויקים בדו"ח המסכם.

---

## 2. שדרוגים מבניים ופדגוגיים לניסויים 1-4

### א. טבלת רכיבים וציוד נדרש (Checklist Table)
* שילבנו בפתח כל ניסוי מעשי (In-Lab) טבלה מובנית ומעוצבת המפרטת את כל הציוד והרכיבים שעל הסטודנט לקחת ממגירות הציוד (נגדים בערכים מתאימים, קבלים, סלילים, סוגי כבלים, ופרובים ייעודיים). הדבר חוסך זמן יקר ומונע התרוצצות חוזרת במהלך המעבדה.

### ב. הטמעת בוחני מוכנות (Moodle Quizzes)
* הטמענו את בוחני מודל הראשיים כ-`iframe` ישירות בדפי סיום ההכנה התיאורטית (Pre-Lab).
* שימוש בפרמטר `embed=1` מאפשר הצגה נקייה של השאלון ללא מעטפת האתר הכללית.
* הוספנו קישור גיבוי לפתיחה בחלון חדש המותאם למכשירי מובייל.

### ג. קישוריות מובנית לציוד המדידה
* שילבנו בגוף הניסויים הפניות וקישורים ישירים לפרקים הרלוונטיים של מכשירי המדידה (הסברים על DMM, כיול מד הספק, שימוש באוסצילוסקופ ועוד).

---

## 3. אוטומציה וניהול קישורים (Configuration-Based System)

כדי לאפשר תחזוקה קלה, שכפול קורסים משנה לשנה (Rollover) והעברה פשוטה בין מחלקות, בנינו מערכת אוטומטית לניהול הקישורים והמזהים:
* **קובץ הגדרות יחיד `moodle_config.json`:** מרכז את מזהי ה-ID שמתקבלים ממודל עבור ספר הציוד והבחנים השונים.
* **ממלאי מקום (Placeholders):** קובצי המקור משתמשים בתגיות כגון `{{MEASURING_EQUIPMENT_BOOK_ID}}` ו-`{{EXP01_PRELAB_QUIZ_ID}}`.
* **סקריפט עדכון אוטומטי `update_moodle_links.py`:** המשתמש מזין את המזהה החדש בפקודה אחת, והסקריפט מעדכן את ה-JSON, מבצע מיגרציה ומפעיל בנייה ואריזה מחדש.
* **סקריפט הקימפול `build.js`:** מחליף את ממלאי המקום בערכים האמיתיים מתוך ה-JSON בעת ייצור הקבצים המוגמרים בתיקיית `dist/`.

---

## 4. שיפורי עיצוב ו-Clean Up (UI/UX)
* **ניקוי מעטפות HTML כפולות:** סרקנו וניקינו את כל קובצי המקור מתגיות HTML, body ו-head כפולות שיצרו התנגשויות תצוגה במודל.
* **עיצוב פרימיום מותאם מודל:** כל הדפים עוצבו מחדש עם פינות מעוגלות, כרטיסים מעוצבים, צללים יוקרתיים, כפתורים דינמיים בעלי אפקטי מעבר (Hover) ודיוק מלא בכיווניות ימין-שמאל (RTL) לשפה העברית.

---

## 5. קובצי הפצה ו-ZIP מוכנים לעבודה
הרצנו את כל תהליך הבנייה, ניקוי ה-dist, אימות הקישורים והאריזה. קובצי ה-ZIP המוכנים להעלאה מיידית למודל ממוקמים בתיקיית `dist/`:
1. `dist\Exp01_Moodle_Distribution.zip` (ניסוי 1)
2. `dist\Exp02_Moodle_Distribution.zip` (ניסוי 2)
3. `dist\Exp03_Moodle_Distribution.zip` (ניסוי 3)
4. `dist\Exp04_Moodle_Distribution.zip` (ניסוי 4)
5. `dist\EELab_Measuring_equipment_Moodle_Book.zip` (ספר ציוד המדידה)
6. `dist\EELab1_Full_Moodle_Distribution.zip` (הפצה מלאה הכוללת את כל החומרים יחד)
